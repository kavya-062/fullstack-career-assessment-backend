spring.application.name=career-assessment-backend

# Server Port - Railway dynamic port with local fallback to 8086
server.port=${PORT:8086}

# Database Configuration - Railway Env Variables with local fallbacks
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/careerdb}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:RKavya@2006}

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Email OTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kavyarapolu999@gmail.com
spring.mail.password=xthhvpgmsqcrqzte
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true